# LLM Guide

Этот документ — основа для стандарта работы с LLM в проекте.

## 1. Цель

- Какие задачи решает модель.
- Какие бизнес-ограничения есть.
- Как выглядит «качественный» ответ.

## 2. Модель и конфигурация

- Модель(и): `TODO`
- Температура: `TODO`
- Ограничения по токенам: `TODO`
- Политики безопасности/модерации: `TODO`

## 3. Формат запросов

- System prompt хранится в `prompts/system.md`.
- User/task шаблоны — в `prompts/task-template.md`.
- Контекст должен быть минимально достаточным и проверяемым.

## 4. Оценка качества

- Список контрольных вопросов (чеклист).
- Набор эталонных кейсов (golden set).
- Метрики (например: accuracy, hallucination rate, latency, cost).

## 5. Политика обновлений

- Любые изменения в промптах логируются в `CHANGELOG.md`.
- Крупные изменения фиксируются в `docs/ROADMAP.md`.
